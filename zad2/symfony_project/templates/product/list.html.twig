
{# templates/product/list.html.twig #}

<h1>Product List</h1>

<ul>
    {% for product in products %}
        <li>
            <a href="{{ path('product_show', {'id': loop.index}) }}">
                {{ product.name }} - ${{ product.price }}
            </a>
        </li>
    {% endfor %}
</ul>

